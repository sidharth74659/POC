{
  "name": "minimal-chatbot",
  "version": "1.0.0",
  "description": "A minimal chatbot that uses DataStax Astra's LangFlow API",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": ">=14.0.0"
  },
  "dependencies": {
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
