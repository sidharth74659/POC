{
  "name": "smart-scheduler-backend",
  "version": "1.0.0",
  "description": "Backend for Smart Scheduler application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "tsc && cd ./dist/example/ && node index.js",
    "build": "tsc && shx chmod +x dist/*.js",
    "watch": "tsc --watch"
  },
  "dependencies": {
    "axios": "^1.9.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "openapi-mcp-converter": "^0.0.5",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.0"
  },
  "devDependencies": {
    "jest": "^29.5.0",
    "nodemon": "^3.0.1",
    "supertest": "^6.3.3"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
